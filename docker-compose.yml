services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: vanopt
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5435:5432"